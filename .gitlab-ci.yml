stages:
  - generateAndBuild

variables:
  CMAKE_GENERATOR: "Ninja"

image: dzmitry/my-runner

buildMiniSpaceSimulator:
  stage: generateAndBuild
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
