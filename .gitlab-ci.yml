stages:
  - generateAndBuild

variables:
  CMAKE_GENERATOR: "Ninja"

image: leanid/fedora_latest

generateGdt:
  stage: generateAndBuild
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
